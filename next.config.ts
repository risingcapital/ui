/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: basePath must match your repo name
  basePath: '/rising-capital',
  // Optional: assetPrefix to ensure static assets work correctly
  assetPrefix: '/rising-capital/',
};

export default nextConfig;
